{
	"name": "html-lite-capture",
	"version": "0.1.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"build:ts": "esbuild src/content/index.ts src/background.ts --bundle --outdir=dist --outbase=src",
		"copy:static": "mkdir -p dist && cp manifest.json dist/manifest.json && cp icon.svg dist/icon.svg",
		"build": "rm -rf dist && npm run build:ts && npm run copy:static",
		"dev": "nodemon --watch src --watch manifest.json --watch icons --ext ts,js,json,png,svg,ico --exec \"npm run build\"",
		"zip": "cd dist && rm -f ../html-lite-capture.zip && zip -r ../html-lite-capture.zip . -x \"__MACOSX/*\" \"*.DS_Store\" \"._*\""
	},
	"keywords": [],
	"author": "vingt-douze <vingt-douze@protonmail.com> (https://vingt-douze.com)",
	"license": "ISC",
	"devDependencies": {
		"esbuild": "^0.27.1",
		"typescript": "^5.9.3"
	},
	"dependencies": {
		"@biomejs/biome": "^2.3.8"
	}
}
